# 代码审查报告 2026-08-15（Kimi，只读）

## 审查范围

最近 7 次 commit（从 `dc1022f` 到 `9aad257`），对应 docs/REVIEW_BY_CLAUDE_CODE.md 第①批「钱和账」修复 + 多 AI 协作基础设施文档/钩子。

- `9aad257` → 🔴-3 删除孤立 JOIN
- `792928c` → 🔴-2 actual_quantity 后端超发闸门
- `a46ffc2` → 🔴-1 老板特批低价发货留痕落库
- `e6a7e52` → 🟡-9 SQLite 镜像补四张明细表自然唯一键
- `e1359c3` → 🟡-5 create_delivery 读合同行加 FOR UPDATE
- `384e821` → 🟡-6 create_delivery 收紧合同状态校验
- `dc1022f` → 协作基础设施（DECISIONS.md / Makefile / hooks / scripts/kimi_review.sh 等）

## 逐 commit 结论

| hash | 条目 | 判定 | 证据（文件:行号） |
|------|------|------|------------------|
| `9aad257` | 🔴-3 删除 `check_delivery_vs_contract` 孤立 JOIN | 贴合 | `tools/local_validator.py:819-821` 已无 `LEFT JOIN delivery_orders d`，`SUM` 不再被 confirmed 发货单数量 K 倍放大 |
| `792928c` | 🔴-2 actual_quantity 后端闸门 | 贴合 | `tools/db_writer.py:1845-1860` 显式传 actual 时必须 `1 <= actual <= pending`；`tools/db_writer.py:1869` 落库用校验后的 `actual`；回写 `delivered_qty` 仍用 `actual_quantity` |
| `a46ffc2` | 🔴-1 price_gap 特批留痕落库 | 贴合 | `sql/01_schema.sql:521-522` 加 `price_gap_approved` / `price_gap_reason`；`tools/local_validator.py:302-303` SQLite 同步；`sample/templates/delivery_orders_template.csv` 同步；`tools/db_writer.py:1886-1887` 写入头行；`tools/db_writer.py:1917-1918` 审计 `new_values` 带 reason；`sql/migrations/2026-08-15_price_gap_approved.sql` 已新增 |
| `e6a7e52` | 🟡-9 SQLite 补四张明细表自然唯一键 | 漏改（R7 未完全同步） | SQLite 侧已补：`tools/local_validator.py:246` `stock_in_items`、`tools/local_validator.py:273` `stock_out_items`、`tools/local_validator.py:327` `delivery_order_items`、`tools/local_validator.py:377` `shipping_record_items`；但 `sql/01_schema.sql:404/456/550/638` 四张明细表仍无 `UNIQUE KEY`，新部署库会丢失幂等锚点 |
| `e1359c3` | 🟡-5 FOR UPDATE 防并发双超发 | 贴合 | `tools/db_writer.py:1788` `SELECT ... FOR UPDATE` 已加；JOIN 的 `sales_contracts` 行也会锁，与审查预期一致 |
| `384e821` | 🟡-6 draft 合同拒发 | 贴合 | `tools/db_writer.py:1797` 改为仅 `confirmed/delivering/completed` 可发货；`cancelled`/`draft` 均被拒 |
| `dc1022f` | 协作基础设施 / 质量门槛 | 贴合 | `DECISIONS.md` 记录分工协议与质量门槛；`Makefile` 定义 `lint/test/validate/check`；`scripts/hooks/pre-commit.sample` 暂存区含 .py 时跑 `make lint`；`scripts/hooks/pre-push.sample` 推送含 .py 时跑 `make check`；`scripts/kimi_review.sh` 创建只读审查入口 |

## 新发现问题

- 🟡 **`sql/01_schema.sql` 未同步 R14 四张明细表自然唯一键，R7 四处同步第 1 处仍缺口**
  - 证据：`sql/01_schema.sql:404` `stock_in_items`、`sql/01_schema.sql:456` `stock_out_items`、`sql/01_schema.sql:550` `delivery_order_items`、`sql/01_schema.sql:638` `shipping_record_items` 均只有自增 `id` 主键，没有 `UNIQUE KEY`。
  - 影响：新环境执行 `sql/01_schema.sql` 建库后，`load-csv-to-db.sh` 的 `REPLACE INTO` 在这四张明细表上退化为纯追加，重复灌库风险复现；与 `sql/migrations/2026-08-14_dedupe_natural_keys.sql` 已在线上库加键的事实也不一致。
  - 建议：在 `01_schema.sql` 的四张明细表上加与 SQLite / 迁移相同的自然唯一键。

- 🔵 **发货单审计 `record_id` 仍可能指向明细表（🔵-16 未在本次修复）**
  - 证据：`tools/db_writer.py:1891-1893` 先 `_doc_insert` 头表，再循环 `_doc_insert` 明细；`tools/db_writer.py:1913` 的 `record_id = cur.lastrowid` 取的是最后一次插入（明细）的 id，而 `write_audit` 的 `table_name='delivery_orders'`。
  - 此问题在审查报告中已列为 🔵-16，本次未修，但第①批范围未包含它，仅作提醒。

## 结论

第①批核心修复（🔴-1/2/3、🟡-5/6）贴合审查建议，可合并；`e6a7e52` 必须补回 `sql/01_schema.sql` 的四张明细表自然唯一键，否则 R7 在新库失效，属于需返工点。
